# SPDX-FileCopyrightText: 2026 Scheidegger Technology GmbH
# SPDX-License-Identifier: AGPL-3.0-only

# Minimal coturn configuration: STUN only.
# TURN support is on the roadmap.

listening-port=3478
listening-ip=0.0.0.0
listening-ip=::

stun-only

# Logging is configured in docker-compose.yml (the image's command-line flags
# override anything set here).
